import { forecastDateFormat } from 'firstchair/helpers/forecast-date-format';

module('ForecastDateFormatHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = forecastDateFormat(42);
  ok(result);
});
