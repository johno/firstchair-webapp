import { wiClassFromWunderground } from 'firstchair/helpers/wi-class-from-wunderground';

module('WiClassFromWundergroundHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = wiClassFromWunderground(42);
  ok(result);
});
