import { googleAnalytics } from 'firstchair/helpers/google-analytics';

module('GoogleAnalyticsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = googleAnalytics(42);
  ok(result);
});
