import { prettyTemperature } from 'firstchair/helpers/pretty-temperature';

module('PrettyTemperatureHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = prettyTemperature(42);
  ok(result);
});
